本周内容：
    day01：文件的高级及函数的基本使用
        1、文件模式
            x模式（控制文件的操作m模式）
                x，只写模式【不可读；不存在则创建，存在则报错】————>了解

            b模式的补充（控制文件读写的内容模式）
                bytes类型转换
                x=10 # int(10)
                '你'.encode('jbk')  # bytes()

        2、操作文件的其他方法

        3、文件的高级操作：控制文件指针的移动

        4、文件修改的两种方式

        5、函数的基本使用
            先定义
                三种定义方式
            后调用int()
                三种调用方式

            返回值：
                三种返回值得方式

    day02:函数的参数

    day03:名称空间与作用域、函数对象、闭包函数（进阶） 难点「尤其注意听」

    day04:无参装饰器、有参装饰器

    day05:可迭代对象：迭代器、生成器