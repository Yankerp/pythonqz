上节内容的复习：
    无参装饰器的魔板：
        def outter(func):
            def wapper(*args, **kwargs):
                res = func(*args, **kwargs)  # 这是我的调用方式
                return res

            return wapper

        @outter
        def index(a, b):
            print(a + b)

        index(1, 3) # 调用方式

    装饰器的目标：
        就是在不改变源代码以及调用方式的前提下增加扩展功能


今日内容：
    有参装饰器：嵌套三层定义的一个函数「需要掌握的一个东西」

    迭代器：主要是用来取值的
        for循环的工作原理

    生成器：yield（自定义的迭代器）

    三元表达式：也可以实现代码写成一行

    生成式：可以实现多行代码写成一行
        列表生成式
        字典生成式
        生成器表达式

